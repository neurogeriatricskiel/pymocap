% test.m
close all; clearvars; clc;

addpath(genpath('/mnt/neurogeriatrics_data/Keep Control/Code/ToolboxCAU'));

data_dir = '/mnt/neurogeriatrics_data/Keep Control/Data/lab dataset';
participant_id = 'pp158';
trial_name = 'walk_fast';

omc_file_name = strcat('omc_', trial_name, '.mat');
imu_file_name = strcat('imu_', trial_name, '.mat');

load(fullfile(data_dir, participant_id, 'optical', omc_file_name));

% Reshape data
pos = reshape(omc_data.pos(:,1:3,:), [size(data.pos,1), (size(data.pos,2)-1)*size(data.pos,3)]);